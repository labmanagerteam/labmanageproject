这个文件主要说明各文件的用处

ａｐａｃｈｅ文件夹里是与ａｐａｃｈｅ服务器连通的的配置文件
ｌａｂｍａｎａｇｅｐｒｏｊｅｃｔ里面是后台的ｐｙｔｈｏｎ脚本
ｍｙ_tag里面是自己创建的模板的标签，实际上只有ｍｙ_tag里的ｔｅｍｐｌａｔｅｓ下的ｄｉｓｐｌａｙ.py文件是有用的
ｓｔａｔｉｃ文件夹下面存的是这个项目下的静态文件
ｔｅｍｐｌａｔｅｓ文件夹下面是这个项目的模板

getchange.sh是这个项目在服务器上的部署脚本

在ｌａｂｍａｎａｇｅｐｒｏｊｅｃｔ里面ｕｒｌｓ.py是所有地址与函数的映射，实际上代码都很简答，只需要从ｕｒｌ开始看起就可以很容易弄懂
context_processors.py文件是一个统一的每个ｒｅｎｄｅｒ时时都会加入的键值对

主要的实线功能的python文件是：
    my_db.py:数据库相关的操作
    lab_manage.py:处理与实验室管理相关的业务逻辑
    lab_view.py:与实验室管理相关的视图（预约与开放计划也在里面）
    usr_manage.py与user_view.py与上面两个类似，只不过时域用户相关的

一些辅助的python文件：
    error_code.py:相关的错误码
    my_check.py:是一些表单输入的验证
    my_decorator.py:视图的装饰，主要做的是登录的验证与权限验证、
    my_exception.py:一些自定义的异常类

一些问题：
    当初为了测试的方便，数据库里的密码是明文，在正式版本中应该加密