一些问题：
    当初为了测试的方便，数据库里的密码是明文，在正式版本中应该加密
    学期的设计上存在问题，现在是只能保存一个学期的信息，需要再加一列，可能还需要加一些验证
    创建循环开放计划有效率上的问题，在界面上点击提交之后会由于后台处理时间过长导致ajax异步的时间已经过了
        这有功能上的原因，因为在这个过程当中需要验证周期性开放计划中的每一个都不与原先的计划冲突，还有一些非空之类的验证都是不可少的，
        所以解决这个问题可能有几种方法：
            1.修改界面部分ajax的异步的时间，是不会出现报错
            2.提高数据库的效率，主要是：
                1.添加索引
                2.修改数据库结构，当初我在写完单次开放计划之后，再写周期计划的时候采取了将周期计划的每次开放都计算出来放在单次计划的表中，这样可以
                复用单次时的函数，但是这样会导致添加周期计划的时间变长
            3.优化下我的python代码可能也是有用的（估计效果不明显）